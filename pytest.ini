[pytest]
testpaths = tests
addopts = -ra -q --tb=short -m "not live"
markers =
    live: requires real model files / network; skipped unless run with -m live
filterwarnings = ignore::DeprecationWarning
